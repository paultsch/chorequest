<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ChoreQuest</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# PWA %>
    <link rel="manifest" href="/manifest.webmanifest">
    <meta name="theme-color" content="#3b82f6">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="default">
    <meta name="apple-mobile-web-app-title" content="ChoreQuest">
    <link rel="apple-touch-icon" href="/apple-touch-icon.png">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gradient-to-br from-white via-blue-50 to-blue-100 min-h-screen font-sans" data-controller="lightbox">
    <!-- Wealthfront-Inspired Top Navigation -->
    <%# Hide top navigation on public child pages (token-based public view) %>
    <% unless controller.controller_name == 'public' %>
    <nav data-controller="nav" class="bg-white/90 backdrop-blur-md border-b border-gray-200 sticky top-0 z-50 font-sans">
      <div class="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between px-8 py-6">
        <div class="flex items-center gap-4 mb-4 md:mb-0">
          <div class="w-12 h-12 bg-gradient-to-br from-blue-500 to-purple-500 rounded-lg flex items-center justify-center text-white font-bold text-2xl shadow">CQ</div>
          <span class="text-3xl font-extrabold text-gray-900 tracking-tight">ChoreQuest</span>
        </div>
        <div class="flex items-center gap-4">
          <!-- Mobile hamburger -->
          <button data-action="click->nav#toggle" data-nav-target="button" aria-expanded="false" class="md:hidden inline-flex items-center justify-center p-2 rounded-md text-gray-600 hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-indigo-500">
            <!-- Hamburger (shown when closed) -->
            <svg class="nav-open-icon h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/></svg>
            <!-- X (shown when open) -->
            <svg class="nav-close-icon hidden h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
          </button>
          <% if admin_user_signed_in? %>
            <div class="flex items-center gap-3">
              <%= link_to super_admin_root_path, class: "px-4 py-2 rounded-full bg-red-50 text-red-700 hover:bg-red-100 font-semibold transition" do %>Dashboard<% end %>
              <%= link_to super_admin_parents_path, class: "px-4 py-2 rounded-full bg-red-50 text-red-700 hover:bg-red-100 font-semibold transition" do %>Parents<% end %>
              <span class="bg-red-100 text-red-700 text-xs font-bold px-2 py-1 rounded-full uppercase tracking-wide">Super Admin</span>
              <%= link_to "Sign Out", destroy_admin_user_session_path, data: { turbo_method: :delete }, class: "px-5 py-2 rounded-full bg-red-600 text-white hover:bg-red-700 font-semibold transition-all duration-200 shadow-sm" %>
            </div>
          <% elsif parent_signed_in? %>
            <div class="flex flex-wrap items-center gap-2">
              <%= link_to admin_root_path, class: "relative px-3 py-2 rounded-full bg-gray-100 text-gray-700 hover:bg-blue-100 hover:text-blue-600 font-semibold transition" do %>
                Dashboard
                <% if @pending_approvals_count&.> 0 %>
                  <span class="absolute -top-1 -right-1 bg-red-500 text-white text-xs font-bold rounded-full h-5 w-5 flex items-center justify-center leading-none">
                    <%= @pending_approvals_count > 9 ? '9+' : @pending_approvals_count %>
                  </span>
                <% end %>
              <% end %>
              <%= link_to children_path, class: "px-3 py-2 rounded-full bg-gray-100 text-gray-700 hover:bg-blue-100 hover:text-blue-600 font-semibold transition" do %>Children<% end %>
              <%= link_to chores_path, class: "px-3 py-2 rounded-full bg-gray-100 text-gray-700 hover:bg-blue-100 hover:text-blue-600 font-semibold transition" do %>Chores<% end %>
              <%= link_to chore_assignments_path, class: "px-3 py-2 rounded-full bg-gray-100 text-gray-700 hover:bg-blue-100 hover:text-blue-600 font-semibold transition" do %>Assignments<% end %>
              <%= link_to token_transactions_path, class: "px-3 py-2 rounded-full bg-gray-100 text-gray-700 hover:bg-blue-100 hover:text-blue-600 font-semibold transition" do %>Tokens<% end %>
              <%= link_to games_path, class: "px-3 py-2 rounded-full bg-gray-100 text-gray-700 hover:bg-blue-100 hover:text-blue-600 font-semibold transition" do %>Games<% end %>
              <%= link_to edit_parent_path(current_parent), class: "px-3 py-2 rounded-full bg-gray-100 text-gray-700 hover:bg-blue-100 hover:text-blue-600 font-semibold transition" do %>Settings<% end %>
              <%= link_to "Sign Out", destroy_parent_session_path, data: { turbo_method: :delete }, class: "px-4 py-2 rounded-full bg-blue-600 text-white hover:bg-blue-700 font-semibold transition-all duration-200 shadow-sm" %>
            </div>
          <% elsif session[:child_id] %>
            <% child = Child.find(session[:child_id]) %>
            <div class="hidden md:flex md:items-center md:gap-4">
              <%= link_to child_path(child), class: "px-4 py-2 rounded-full bg-gray-100 text-gray-700 hover:bg-green-100 hover:text-green-600 font-semibold text-lg transition" do %>Home<% end %>
              <%= link_to games_path, class: "px-4 py-2 rounded-full bg-gray-100 text-gray-700 hover:bg-green-100 hover:text-green-600 font-semibold text-lg transition" do %>Games<% end %>
            </div>
            <span class="hidden md:inline text-gray-700 font-medium"><%= child.name %></span>
            <div class="hidden md:inline">
              <%= link_to destroy_child_session_path, method: :delete, class: "px-6 py-2 rounded-full bg-gray-100 text-gray-800 hover:bg-gray-200 font-semibold text-lg transition-all duration-200 shadow-sm" do %>Sign Out<% end %>
            </div>
            <div data-nav-target="menu" class="hidden md:hidden absolute left-0 right-0 bg-white border-t border-gray-100 shadow-md">
              <div class="px-6 py-4 space-y-2">
                <%= link_to child_path(child), class: 'block px-3 py-2 rounded text-gray-700 hover:bg-gray-50' do %>Home<% end %>
                <%= link_to games_path, class: 'block px-3 py-2 rounded text-gray-700 hover:bg-gray-50' do %>Games<% end %>
                <div class="pt-2"><%= link_to destroy_child_session_path, method: :delete, class: 'w-full text-left px-3 py-2 rounded bg-gray-100' do %>Sign Out<% end %></div>
              </div>
            </div>
          <% else %>
            <div class="hidden md:flex md:items-center md:gap-4">
              <a href="#features" class="px-4 py-2 rounded-full bg-gray-100 text-gray-700 hover:bg-blue-100 hover:text-blue-600 font-semibold text-lg transition">Features</a>
              <a href="#pricing" class="px-4 py-2 rounded-full bg-gray-100 text-gray-700 hover:bg-blue-100 hover:text-blue-600 font-semibold text-lg transition">Pricing</a>
              <%= link_to games_path, class: "px-4 py-2 rounded-full bg-gray-100 text-gray-700 hover:bg-blue-100 hover:text-blue-600 font-semibold text-lg transition" do %>Games<% end %>
              <a href="#about" class="px-4 py-2 rounded-full bg-gray-100 text-gray-700 hover:bg-blue-100 hover:text-blue-600 font-semibold text-lg transition">About</a>
              <a href="#blog" class="px-4 py-2 rounded-full bg-gray-100 text-gray-700 hover:bg-blue-100 hover:text-blue-600 font-semibold text-lg transition">Blog</a>
              <a href="#contact" class="px-4 py-2 rounded-full bg-gray-100 text-gray-700 hover:bg-blue-100 hover:text-blue-600 font-semibold text-lg transition">Contact</a>
            </div>
            <%= link_to "Parent Login", new_parent_session_path, class: "hidden md:inline px-6 py-2 rounded-full bg-blue-600 text-white hover:bg-blue-700 font-semibold text-lg transition-all duration-200 shadow-sm" %>
            <%= link_to "Child Login", new_child_session_path, class: "hidden md:inline px-6 py-2 rounded-full bg-blue-600 text-white hover:bg-blue-700 font-semibold text-lg transition-all duration-200 shadow-sm" %>
            <div data-nav-target="menu" class="hidden md:hidden absolute left-0 right-0 bg-white border-t border-gray-100 shadow-md">
              <div class="px-6 py-4 space-y-2">
                <a href="#features" class="block px-3 py-2 rounded text-gray-700 hover:bg-gray-50">Features</a>
                <a href="#pricing" class="block px-3 py-2 rounded text-gray-700 hover:bg-gray-50">Pricing</a>
                <%= link_to games_path, class: 'block px-3 py-2 rounded text-gray-700 hover:bg-gray-50' do %>Games<% end %>
                <a href="#about" class="block px-3 py-2 rounded text-gray-700 hover:bg-gray-50">About</a>
                <a href="#blog" class="block px-3 py-2 rounded text-gray-700 hover:bg-gray-50">Blog</a>
                <a href="#contact" class="block px-3 py-2 rounded text-gray-700 hover:bg-gray-50">Contact</a>
                <div class="pt-2"><%= link_to new_parent_session_path, class: 'w-full text-left px-3 py-2 rounded bg-blue-600 text-white' do %>Parent Login<% end %></div>
                <div><%= link_to new_child_session_path, class: 'w-full text-left px-3 py-2 rounded bg-blue-600 text-white' do %>Child Login<% end %></div>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    </nav>
    <% end %>
    <!-- Main Content -->
    <main class="min-h-[calc(100vh-64px)]">
      <div class="max-w-7xl mx-auto px-4 md:px-8 py-8">
        <% if notice %>
          <div class="mb-6 p-4 bg-green-50 border-l-4 border-green-500 text-green-700 rounded-lg">
            <%= notice %>
          </div>
        <% end %>
        <% if alert %>
          <div class="mb-6 p-4 bg-red-50 border-l-4 border-red-500 text-red-700 rounded-lg">
            <%= alert %>
          </div>
        <% end %>
        <%= yield %>
      </div>
    </main>

    <%# PWA install prompt banner (Chrome/Edge/Android only — invisible on iOS) %>
    <div data-controller="pwa-install">
      <div data-pwa-install-target="banner"
           class="hidden fixed bottom-4 left-4 right-4 md:left-auto md:right-4 md:w-80
                  bg-white rounded-2xl shadow-xl border border-gray-100 p-4 z-50">
        <div class="flex items-center gap-3 mb-3">
          <div class="w-10 h-10 bg-gradient-to-br from-blue-500 to-purple-500 rounded-xl
                      flex items-center justify-center text-white font-bold text-sm flex-shrink-0">CQ</div>
          <div>
            <p class="font-semibold text-gray-900 text-sm leading-tight">Install ChoreQuest</p>
            <p class="text-gray-500 text-xs">Add to your home screen</p>
          </div>
        </div>
        <div class="flex gap-2">
          <button data-action="pwa-install#install"
                  class="flex-1 bg-blue-600 text-white text-sm font-semibold py-2 rounded-full
                         hover:bg-blue-700 transition-colors">
            Install
          </button>
          <button data-action="pwa-install#dismiss"
                  class="px-4 text-gray-500 text-sm hover:text-gray-700 transition-colors">
            Not now
          </button>
        </div>
      </div>
    </div>

    <!-- Footer -->
    <footer class="bg-white border-t border-gray-200 mt-12">
      <div class="max-w-7xl mx-auto px-6 py-8 flex flex-col md:flex-row items-center justify-between gap-4">
        <div class="flex items-center gap-2">
          <div class="w-8 h-8 bg-gradient-to-br from-blue-500 to-purple-500 rounded-lg flex items-center justify-center text-white font-bold text-sm">CQ</div>
          <span class="text-gray-700 font-semibold">ChoreQuest</span>
        </div>
        <div class="text-gray-500 text-sm">&copy; <%= Time.now.year %> ChoreQuest. All rights reserved.</div>
      </div>
    </footer>
  <%# Lightbox overlay — driven by lightbox_controller.js %>
  <div data-lightbox-target="overlay"
       data-action="click->lightbox#closeOnBackdrop"
       class="hidden fixed inset-0 z-[9999] bg-black/80 flex items-center justify-center p-4 backdrop-blur-sm">
    <button type="button"
            data-action="click->lightbox#close"
            class="absolute top-4 right-4 text-white text-3xl font-bold w-10 h-10 flex items-center justify-center rounded-full bg-black/40 hover:bg-black/60 transition">
      &times;
    </button>
    <img data-lightbox-target="image" src="" alt="Chore photo"
         class="max-w-full max-h-[90vh] object-contain rounded-lg shadow-2xl" />
  </div>

  <%# PWA Service Worker registration.
      The 'serviceWorker' in navigator guard ensures this silently no-ops inside
      iOS WKWebView and Android WebViews used by Turbo Native — no conflict. %>
  <script>
    if ('serviceWorker' in navigator) {
      window.addEventListener('load', function() {
        navigator.serviceWorker.register('/service-worker.js', { scope: '/' });
      });
    }
  </script>
  </body>
</html>
